# Mexi Swap

Repositorio del frontend y contratos de Mexi Swap.

Instalación rápida:

```
pnpm install
pnpm dev
```

Estructura principal:
- `client/`: frontend (Vite + React + TypeScript)
- `contracts/`: contratos Solidity

Contacto: Rex-Flowers
